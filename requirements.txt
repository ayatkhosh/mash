# requirements.txt
# UrbanInsight - Building Detection & Segmentation
# المكتبات المطلوبة لتشغيل المشروع

# ── المكتبات الأساسية / Core Libraries ──────────────────────────────────
numpy>=1.24.0
matplotlib>=3.7.0
Pillow>=9.5.0
PyYAML>=6.0

# ── معالجة الصور / Image Processing ────────────────────────────────────
opencv-python-headless>=4.7.0
rasterio>=1.3.0
scipy>=1.10.0

# ── نماذج الذكاء الاصطناعي / AI Models ─────────────────────────────────
# تُثبَّت عبر الخلية الأولى في الـ Notebook لأنها تحتاج إعدادات خاصة
# Installed via Cell 1 in the Notebook as they need special setup:
#   - GroundingDINO: git clone https://github.com/IDEA-Research/GroundingDINO
#   - SAM: pip install git+https://github.com/facebookresearch/segment-anything

# ── Hugging Face / Transformers ──────────────────────────────────────────
transformers==4.41.2
timm>=0.9.0

# ── أدوات كشف الأجسام / Object Detection Tools ──────────────────────────
supervision>=0.19.0

# ── بيانات SpaceNet / SpaceNet Data ─────────────────────────────────────
kagglehub>=0.2.0

# ── PyTorch (يُثبَّت مسبقاً في Colab) ──────────────────────────────────
# torch>=2.0.0
# torchvision>=0.15.0
# تحذير: لا تقم بتثبيت torch عبر requirements.txt على Colab
# Warning: Do NOT install torch via requirements.txt on Colab
# (Colab comes with a pre-installed compatible version)
